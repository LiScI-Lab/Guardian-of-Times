= render 'application/form_errors', errors: @progress.errors

= simple_form_for([@project, @progress]) do |f|
  .row
    .col.s10
      = f.input :tags, wrapper_html: {class: 's6'}
      = f.input :description, wrapper_html: {class: 's6'}
    .col.s2
      = f.button :button
        = mdi_tag 'run-fast'